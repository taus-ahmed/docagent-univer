# Test and lint dependencies. NOT installed in the production image —
# Dockerfile.backend installs requirements.txt only.
#
#   pip install -r requirements.txt -r requirements-dev.txt
#
# pyflakes is not optional: tests/test_no_undefined_names.py FAILS when it is
# missing rather than skipping, because a check that cannot run must not report
# success. One undefined name shipped a total-failure bug in Phase 8.
-r requirements.txt

pytest==8.2.2
pyflakes==3.2.0
